# Server requirements
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
alembic>=1.13
psycopg[binary]>=3.1.0
pydantic[email]>=2.6.0
python-dotenv>=1.0

# Google OAuth — verificare id_token + HTTP client
google-auth>=2.30
requests>=2.31
httpx>=0.27

# PDF reports
reportlab>=4.0

# Rate limiting (anti brute-force pe /auth/login, /auth/register)
slowapi>=0.1.9

# Dev / tests
pytest>=8.0.0
pytest-asyncio>=0.23
