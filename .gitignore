# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
.venv-build/
*.egg-info/

# Agent - contine token-uri secrete
agent/config.ini

# Agent Google OAuth — local config (nu committe-uim client_id-ul desktop in repo public)
agent/google_config.py

# Server
server/.env
server/.venv/

# Build artifacts (PyInstaller output)
build/
dist/
server/app/static/agent/*.exe
server/static/agent/*.exe

# Pytest
.pytest_cache/

# Logs locale
*.log

# Baze SQLite locale / temporare (ex: rulari alembic autogenerate)
*.db
*.sqlite
*.sqlite3

# Prezentare licenta — se tine local, nu in repo
*.pptx
~$*

# OS
.DS_Store
Thumbs.db

# Claude Code + superpowers (artefacte locale, nu fac parte din proiect)
.claude/
.superpowers/
.pytmp/
