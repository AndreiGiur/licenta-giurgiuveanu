# memory.md — agent/tests/

Smoke tests pentru `core.py`. Testeaza partea **fara network/UI**:
config roundtrip, structura datelor colectate, helperi de enrollment cu
mock-uri pentru apelurile HTTP.

Rulare: `python -m pytest agent/tests` (din radacina repo-ului).

## Fisiere

| Fisier                  | Rol                                                                  |
| ----------------------- | -------------------------------------------------------------------- |
| `__init__.py`           | Marcheaza directorul ca pachet pytest.                               |
| `test_core.py`          | 12 teste: `is_enrolled`, `save_enrollment`/`get_enrollment` roundtrip, `clear_config` idempotent, `get_enrollment` ridica `RuntimeError` cand lipseste configul, `collect_system_data` smoke test (structura), `executable_path` returneaza `Path`, `is_frozen` returneaza `bool`, `perform_enrollment` propaga `ApiError`, **+4 teste `_run_nmap_if_needed`** (standard returneaza None, advanced fara nmap returneaza `{error: nmap_missing}`, deep fara nmap returneaza `{error: nmap_missing}`, alias `_run_nmap_if_deep` === `_run_nmap_if_needed`). **+3 (2026-06-01):** `collect_system_data` scaleaza progresul la `max_progress` (monoton, <=65), `run_one_job` cere max_progress=65 pentru deep, `build_heartbeat_payload` include `net_bytes_sent/recv/conn_count`. Foloseste `monkeypatch` pentru a redirectiona `CONFIG_DIR`/`CONFIG_FILE` in `tmp_path`. |
| `test_core_relink.py`   | 9 teste pentru smart re-link: `save_enrollment` cu metadata (`device_name`, `user_email`), `get_enrollment_meta` cu sau fara config, `login_or_register` cu fallback la register pe 401, `login_or_register` propaga erori non-401, `api_get_device_by_uid` returneaza `None` pe 404 si propaga alte erori, `enroll_device_with_session` ramuri pentru relink vs create. Foloseste `unittest.mock.patch.object` pentru a stuba apelurile HTTP. |
| `test_collectors.py`    | 15 teste (+2 `collect_network_identity` 2026-06-02: IP+MAC interfata activa via mock psutil AF_LINK; `{}` cand doar loopback): `SCAN_PROFILES` are 3 nivele cu flag-uri corecte (standard minimal, advanced + persistence, deep + forensics); colectorii din `agent/collectors/` returneaza structurile asteptate (`open_ports`, `connections` pe advanced, `cmdline` pe advanced+, etc.); `collect_system_data(scan_type)` orchestreaza corect; `progress_cb` este apelat intre colectori cu progres in [0, 100]. |
| `test_google_oauth.py`  | 2 teste mock pentru `api_google_enroll`: `test_api_google_enroll_sends_token_hash` (POST cu body care contine `id_token` + `token_hash`, raspuns FARA `device_token`) + propagare `ApiError`. |
| `test_daemon_recovery.py` | 3 teste pentru `daemon_loop` auto-recovery la 401: `test_daemon_invalid_token_exits_after_first_401` (heartbeat 401 → `on_token_invalid` apelat, loop iese imediat), `test_daemon_network_error_keeps_running` (ApiError → continua retry, callback NU apelat), `test_daemon_get_job_401_also_triggers_recovery` (heartbeat OK dar `api_get_next_job` da 401 → recovery). |
| `test_metrics_tracker.py` | 5 teste pentru `MetricsTracker`: state gol cand fisier lipseste, `record_scan` persista atomic + actualizeaza `scans_total`/`last_*`/history, history capped la 20, JSON corupt fallback la state gol (fara crash), `reset()` sterge fisierul. Logica pura — fara network sau UI. |
| `test_ipc.py`           | 3 teste pentru protocol IPC localhost (TCP socket port 47815): request/response cu handler default, push events catre subscribers, eroare pe cmd necunoscut. Foloseste fixture `server_port` (closed before yield ca SO_REUSEADDR sa permita reconectare pe Windows). |
| `test_nmap_runner.py`   | 18 teste pentru `agent/nmap_runner.py` (**+3 (2026-06-01):** `parse_nmap_stats_line` extrage percent+ETA / intoarce None pe linii fara progres; `--stats-every 2s` prezent in args la profile). 15 teste pentru `agent/nmap_runner.py`: `validate_cidr` accepta forme valide, `validate_lan_target` refuza public IP + subnet > 4096 hosts, `build_nmap_args` legacy (`-sV -O`, top-1000, all-ports `-p-`). **+8 teste NMAP_PROFILES**: profiles exista (advanced + deep cu chei obligatorii), advanced are args moderati (`-sV -T4 --top-ports 5000`, fara `-A`), deep are args agresivi (`-A -Pn -T4 --version-intensity 9 --top-ports 5000 + vuln,default,auth,banner`), deep mai agresiv decat advanced (superset scripturi), legacy backwards-compat fara profile (default `-sV -O`), profile necunoscut → legacy fallback, advanced timeout < deep timeout, all-ports override --top-ports din profil. |
| `test_nmap_install.py`  | 9 teste **(2026-06-01)** pentru instalarea nmap: `detect_package_manager` (apt/pacman/none via monkeypatch `shutil.which`); `build_nmap_install_command` per OS (winget pe Windows, apt/pacman pe Linux, None cand nu stim); `install_nmap` fallback la comanda manuala cand nu exista pkexec/sudo + unknown OS. |
| `test_nmap_parser.py`   | 3 teste pentru `agent/nmap_parser.py`: parse fixture `nmap_localhost.xml`, extract JSON din `<script id="vulnwatch-audit">`, `NmapParseError` pe XML invalid. |
| `test_linux_audit.py`   | **8 teste (2026-06-01)** pentru functiile pure din `agent/collectors/linux_audit.py`: `_parse_sshd_config` (permit_root_login/empty/password_auth/x11 din text mock + valori implicite cand lipsesc), `_uid0_accounts` (extrage conturi cu UID 0 din `/etc/passwd` mock), `_empty_password_accounts` (conturi cu camp parola gol in `/etc/shadow`), `_parse_sysctl` (ip_forward/aslr/suid_dumpable), `_parse_login_defs` (pass_max_days/umask), `_tmp_missing_noexec` (din continut `/proc/mounts`), `_filter_suid` (pastreaza doar binarele in afara setului `KNOWN_SUID`), `_sudo_nopasswd`. Fara subprocess — primesc text mock. |
| `test_collectors_rich.py` | **14 teste (Task 1+2+3+4+5)** pentru colectorii registry/PS noi din `system_info.py` + flag-uri ScanProfile: `test_uac_status_reads_registry_values`, `test_uac_status_missing_keys_returns_empty`, `test_autologon_password_present_without_reading_value` (assert non-tautologic: nici o cheie cu "password" in afara de `password_present`), `test_autologon_absent`, `test_smb1_explicit_enabled`, `test_smb1_missing_key_returns_none`, `test_local_users_includes_password_required` (monkeypatch `_ps`, verifica `password_required` + `is_admin`), `test_new_profile_flags_per_level` (verifica `include_wifi_profiles`/`include_password_policy`/`include_audit_policy` la toate 3 niveluri). **(Task 4) +3:** `test_parse_secedit_inf_extracts_numeric_keys` (fixture INF cu sectiuni System Access + Event Audit, verifica 5 chei numerice), `test_parse_secedit_inf_garbage_returns_empty` (text invalid → `{}`), `test_collect_system_splits_password_and_audit_policy` (monkeypatch `_secedit_export`, verifica split password_policy vs audit_policy in output `collect_system`; Windows-only). **(Task 5) +1:** `test_defender_status_includes_exclusions` (monkeypatch `_ps` cu Get-MpPreference JSON; verifica paths=lista, processes scalar→lista, extensions null→lista goala). |
| `test_single_instance.py` | 7 teste pentru `agent/single_instance.py` (garda instanta unica): prima `acquire` reuseste, a doua instanta cu acelasi nume esueaza, `release` permite re-acquire, nume diferite nu se ciocnesc, `acquire` idempotent pe acelasi obiect, context manager achizitioneaza/elibereaza, `release` fara acquire e safe. Nume unice via `uuid` per test. Pe Windows ruleaza calea mutex (pywin32), pe Linux CI calea fallback `fcntl`. |

## Pattern

Toate testele sunt **fara network**:
- Apelurile HTTP sunt mock-uite cu `mock.patch.object(core, "api_login", ...)`.
- Configul scrie/citeste in `tmp_path` (fixture `tmp_config_dir`/`tmp_config`).
- `collect_system_data` ruleaza efectiv pe sistemul curent (citeste procese reale)
  dar verifica doar **structura** raspunsului, nu valori specifice.

Testele de end-to-end (cu HTTP real catre backend) sunt in `server/tests/`.

**Total: 131 teste** (include +8 `test_linux_audit.py`; 2026-06-01: 3 `_detect_local_subnet` in test_core, 2 MAC in test_nmap_parser, 3 subnet/-Pn in test_nmap_runner; 2026-06-02: 2 `collect_network_identity` in test_collectors; Task 1 +6 `test_collectors_rich.py` UAC/autologon/SMB1; Task 2 +1 `test_collectors_rich.py` password_required; Task 3 +1 `test_collectors_rich.py` flag-uri ScanProfile wifi/password_policy/audit_policy; Task 4 +3 `test_collectors_rich.py` secedit parse + collect_system split policy; Task 5 +1 `test_collectors_rich.py` defender exclusions paths/processes/extensions).
