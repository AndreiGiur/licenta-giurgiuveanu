# Runtime
psutil>=5.9.0
requests>=2.31.0

# GUI / tray (Tkinter este inclus in Python pe Windows)
pillow>=10.0.0
pystray>=0.19.5

# Google OAuth (loopback flow pentru enrollment cu cont Google)
google-auth-oauthlib>=1.2
google-auth>=2.30
