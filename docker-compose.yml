services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: exposure
      POSTGRES_PASSWORD: exposure
      POSTGRES_DB: exposure
    ports:
      - "5432:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data

volumes:
  dbdata:
